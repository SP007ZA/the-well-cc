query GetEvent($where: EventWhereUniqueInput!) {
  event(where: $where) {
    id,
    title,
    description,
    price,
    startDate,
    endDate,
    eventThumbnail {
      image {
        publicUrlTransformed
      }
    },
    address {
       fullAddress
    }
  }
}