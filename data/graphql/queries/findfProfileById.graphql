query Profile($where: ProfileWhereUniqueInput!) {
  profile(where: $where) {
    id
    firstName
    lastName
     cellNumber
    user {
      id
      email
      membership {
        id
       
      }
    }
  }
}